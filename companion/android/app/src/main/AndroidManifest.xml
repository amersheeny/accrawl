<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Capture incoming bank SMS only while a concrete crawl session has armed the native relay. READ_SMS
         permits the active-session inbox observer fallback when a device suppresses the receive broadcast. -->
    <uses-permission android:name="android.permission.RECEIVE_SMS"/>
    <uses-permission android:name="android.permission.READ_SMS"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Camera is used only by the in-app pairing scanner. The QR contains the console address and a
         short-lived pairing request secret; durable credentials are issued only after the operator compares
         the verification code and approves the phone in the console. Permission is requested at scan time.
         Camera-less devices can enter the same short-lived request details by hand. -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>

    <application
        android:label="Accrawl Companion"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:usesCleartextTraffic="${usesCleartextTraffic}">
        <meta-data
            android:name="app.accrawl.source_revision"
            android:value="${sourceRevision}" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Data-only high-priority FCM wakes the app for one concrete OTP/tunnel session. -->
        <service
            android:name=".CompanionFcmService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Active only while one or more concrete crawl sessions need SMS relay. -->
        <service
            android:name=".RelayService"
            android:exported="false"
            android:foregroundServiceType="dataSync"/>

        <!-- Active only while one or more concrete crawls route through this phone. -->
        <service
            android:name=".TunnelService"
            android:exported="false"
            android:foregroundServiceType="dataSync"/>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
