# Content-strategist review

- Run: `019f9bd7-ad66-7131-b816-b8f1290db269`
- Surface: connection deletion HTTP 409 response
- Internal states: `running` or `cancelling`
- Proposed message: `Wait for the crawl to finish or stop before deleting this connection.`
- Context reviewed: the connection remains locked while a crawl is running or
  while its worker resources are being fenced during cancellation.

The independent reviewer checked lifecycle accuracy, clarity, actionability,
tone, and consistency with the product nouns `connection` and `crawl`.

## Verdict

The message is lifecycle-accurate, clear, actionable, neutral in tone, and
consistent with the product nouns.

`READY`
