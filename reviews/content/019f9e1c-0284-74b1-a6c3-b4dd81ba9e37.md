# Companion documentation copy review

- Review run: `019f9e1c-0284-74b1-a6c3-b4dd81ba9e37`
- Reviewer: independent content-strategist review (`gpt-5.6-sol`, ultra reasoning, read-only)
- Scope: revised companion summary, pairing, security, financial-data, OTP, tunnel, build, and E2E documentation
- Verdict: **READY**

The final pass reviewed the revised documentation as a single set after an
earlier pass required consistent use of the product nouns **Accrawl console**,
**Companion**, **connection**, **crawl**, and **phone**, plus more precise
pairing, revocation, financial-data, OTP, build, and validation claims.

The accepted text states that:

- pairing is limited to selected connections, expires after five minutes, and
  returns the two final credentials only once after code comparison and
  approval;
- Companion shows balances and transactions, keeps financial responses in
  memory, relocks on background, and requires HTTPS for production financial
  traffic;
- transaction similarity is never treated as proof that two records are the
  same;
- the phone forwards sender-bound SMS content while the Accrawl console
  extracts the code;
- the documented emulator E2E covers the rendered financial states, native SMS
  relay, browser crawl, lifecycle relock, revocation, and secure screenshot
  blocking; and
- secure is the default build flavor, QA is local-only, and release signing
  cannot fall back to the debug key.

`READY`
