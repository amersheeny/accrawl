# E2E companion-flow documentation review

- Run: `019f9e93-76cc-7e20-a503-af6f38515cb4`
- Reviewer: independent content-strategist pass (`gpt-5.6-sol`, ultra)
- Scope: `e2e/README.md`, companion relay mode
- Verdict: approved after the corrections recorded below

## Correctness findings

1. “Configures a device credential” conflated Android screen-lock PIN setup with the subsequent Accrawl
   device-credential flow.
2. “Screen-lock-bound credential flow” and the unqualified “unlocks” did not say which credential the
   Android PIN authenticates.
3. “Verifies that the Accrawl console revoked both linked credentials” named the wrong verification
   surface; the harness checks PostgreSQL.

## Approved resolution

The replacement passage now names Android screen-lock PIN setup separately, explains where that PIN is used,
and states that self-revocation is initiated in the Companion app and confirmed in PostgreSQL.
