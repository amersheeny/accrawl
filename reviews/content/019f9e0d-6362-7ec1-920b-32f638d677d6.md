# Crawl and pagination copy review

- Review run: `019f9e0d-6362-7ec1-920b-32f638d677d6`
- Reviewer: independent content-strategist review (`gpt-5.6-sol`, ultra reasoning, read-only)
- Scope: crawl pre-dispatch failures, crawl completion deadline, device-proxy availability, and companion pagination errors
- Verdict: **READY**

The review checked that user-visible errors do not expose internal enum values,
implementation terminology, or secret environment-variable names; that the same
condition uses the same sentence across the stored connection failure, crawl
session, and returned API result; and that each sentence remains factually
specific without promising an unavailable self-service remedy.

## Exact approved catalogue

- `This connection must be enabled and signed in before the crawl can start.`
- `This institution’s login domain hasn’t been verified, so the crawl can’t start.`
- `The saved login address doesn’t match this institution’s domain.`
- `The crawl can’t start until its safety check passes.`
- `Accrawl isn’t configured to route crawls through a phone’s network.`
- `None of the paired phones is allowed to crawl this connection.`
- `The crawl didn’t finish within the allowed time.`
- `The page request is invalid. Refresh and try again.`

The pagination sentence is approved for all six identical occurrences in
`apps/control-plane/src/routes/companion.ts`: three endpoints each reject both
an invalid query shape and an invalid decoded cursor.

`READY`
